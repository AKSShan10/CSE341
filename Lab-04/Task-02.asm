;LAB-05 TASK-02
.MODEL SMALL

.STACK 100H

.DATA
 
SUM DW ?

POS DW ?

       
.CODE
MAIN PROC

;initizlize DS

MOV AX,@DATA
MOV DS,AX

; enter your code here

MOV POS,0
MOV SUM,0

MOV CX,1
;MOV DX,8
;MOV DX,8

WHILE:

MOV AH,0
MOV AX,POS
CMP CX,1024        ;8;16;32;256;512
JG end 
MOV BL,2
DIV BL 
;MOV BH,0
;ADD BH,30H             
CMP AH,0H 
JE EVEN_POSITION
ADD SUM,CX
JMP INCREMENT


EVEN_POSITION:
SUB SUM,CX

INCREMENT:
MOV BL,2
MOV AX,CX
MUL BX
MOV CX,AX
INC POS

JMP WHILE ; CALLING THE LOOP

end:

MOV BX,SUM
ADD BX,30H
MOV AH,2
MOV DX,BX
INT 21H





;exit to DOS
MOV AX,4C00H
INT 21H

MAIN ENDP
   END MAIN